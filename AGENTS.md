# Agent instructions

## Agent skills

### Issue tracker

Issues and specifications are tracked in GitHub Issues for `PointMountain/game-account-select`. See `docs/agents/issue-tracker.md`.

### Triage labels

Use the repository's standard five-state triage labels. See `docs/agents/triage-labels.md`.

### Domain docs

This repository uses a single domain context. Read the root `CONTEXT.md` and relevant records under `docs/adr/` when they exist. See `docs/agents/domain.md`.
